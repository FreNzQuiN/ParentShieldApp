import { css } from '@emotion/css';

export const sTitle = css`
  color: #20311a;
  font-family: Poppins;
  font-size: 28px;
  font-style: normal;
  font-weight: 700;
  line-height: 1.1;
  letter-spacing: -0.02em;
`;
